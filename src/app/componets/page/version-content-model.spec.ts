import { VersionContentModel } from './version-content-model';

describe('VersionContentModel', () => {
  it('should create an instance', () => {
    expect(new VersionContentModel()).toBeTruthy();
  });
});
